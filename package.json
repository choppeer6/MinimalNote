{
  "name": "minimalnote-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "极简小记 · 数据层纯逻辑核心(不参与 App 打包)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
